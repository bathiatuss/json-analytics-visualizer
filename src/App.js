import UserDataDetailsComponent from "./components/UserDetailsComponent";

function App() {
  return (
    <div
      style={{
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        textAlign: "center",
      }}
    >
      <UserDataDetailsComponent />
    </div>
  );
}

export default App;
